name: Dizipal Veri Çekici 🤖

on:
  workflow_dispatch: # Manuel çalıştırma butonu ekler
  schedule:
    - cron: '0 */6 * * *' # Her 6 saatte bir otomatik çalışır

permissions:
  contents: write

jobs:
  scrape_and_commit:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Repoyu Çek (Checkout)
        uses: actions/checkout@v3

      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🌐 Google Chrome Kurulumu
        uses: browser-actions/setup-chrome@latest

      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Botu Çalıştır
        run: python main.py

      - name: 💾 Verileri Kaydet ve Pushla
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          git add diziler.json
          git commit -m "🤖 Yeni veriler eklendi: $(date)" || echo "Değişiklik yok"
          git push
