name: Dizi Verilerini Çek (Scraper Bot)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu Klonla (Checkout)
        uses: actions/checkout@v3

      - name: Python 3.10 Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Gerekli Python Kütüphanelerini Yükle
        run: |
          python -m pip install --upgrade pip
          pip install beautifulsoup4 curl_cffi

      - name: Scraper Botunu Çalıştır
        run: python scraper.py

      - name: JSON Sonucunu GitHub'a Kaydet (Commit & Push)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add dizi_verileri.json
          git commit -m "🤖 Dizi/Film verileri güncellendi" || echo "Güncellenecek yeni veri yok."
          git push
