name: TRT Cocuk M3U Olusturucu

on:
  schedule:
    - cron: '0 */12 * * *' # Her 12 saatte bir otomatik çalışır
  workflow_dispatch: # Actions sekmesinden manuel tetiklemeye izin verir

permissions:
  contents: write # Bot'un repoya dosya yazabilmesi için gerekli izin

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Repoyu Klonla
      uses: actions/checkout@v3

    - name: Python 3.x Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Gerekli Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Verileri Çek ve M3U Dosyasını Oluştur
      run: python scraper.py

    - name: Değişiklikleri Kaydet (Commit & Push)
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add trtcocuk.m3u
        # Sadece dosyada bir değişiklik varsa commit atar, yoksa hata vermeden atlar
        git commit -m "🤖 M3U listesi güncellendi (Otomatik)" || echo "Değişiklik yok, commit atlanıyor."
        git push
