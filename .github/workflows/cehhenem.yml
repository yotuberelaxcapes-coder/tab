name: TRT Cocuk Dinamik M3U Guncelleyici

on:
  schedule:
    - cron: '0 */12 * * *' # Her 12 saatte bir çalışır
  workflow_dispatch: # GitHub üzerinden manuel tetiklemeye izin verir

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Repoyu Klonla
      uses: actions/checkout@v3

    - name: Python 3.10 Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Gerekli Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Kazıyıcıyı Çalıştır
      run: python scraper.py

    - name: Değişiklikleri Kaydet (Commit & Push)
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add trtcocuk.m3u
        # Sadece dosyada bir değişiklik varsa commit atar, yoksa script hatasız şekilde kapanır
        git commit -m "🤖 TRT Çocuk M3U listesi güncellendi (Otomatik)" || echo "Değişiklik yok, commit atlanıyor."
        git push
